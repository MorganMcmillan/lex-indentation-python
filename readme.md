# Indentation Lexer in Python

This is a quick little practice I did to figure out how to lex `indent` and `dedent` tokens for a future indentation-based language like Python.

# TODO

- [x] Lex `indent`, `dedent`, and `newline` tokens
- [x] Ignore blank lines
- [x] Implement indentation ignoring for operator tokens (I.e. `+`, `-`, `*`, `/`) and the blank `\` token